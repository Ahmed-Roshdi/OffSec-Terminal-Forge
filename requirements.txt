# OffSec-Terminal-Forge — Python dependencies
# Install: pip install -r requirements.txt

Pillow>=10.0.0       # Image generation, manipulation, WebP export
requests>=2.31.0     # Groq / OpenRouter API calls
numpy>=1.24.0        # Fast pixel-sort in core_engine (vectorised row ops)